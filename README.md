# ci-agent-actions-testing
testing repository for ci agent actions for sacrificial commits, tags, etc.

## integration testing
- make changes to the ci-agent on a development branch
- create a development branch in the ci-agent-actions repository and point the ci-agent tag to the development branch docker tag
- create a development branch here and test a release